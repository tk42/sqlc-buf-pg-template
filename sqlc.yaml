version: 2
sql:
  - engine: "postgresql"
    schema: "petapis/schema"
    queries: "petapis/query"
    gen:
      go:
        package: "petapis"
        out: "gen/sqlc"

    # emit_json_tags: true
    # emit_prepared_queries: false
    # emit_interface: true
    # emit_exact_table_names: false
    # emit_empty_slices: true
